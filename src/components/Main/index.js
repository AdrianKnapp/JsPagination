import React from "react";

import Items from "../Items";
import Pagination from "../Pagination";

const Main = () => {
  return (
    <main>
      <Items />
      <Pagination />
    </main>
  );
};

export default Main;
